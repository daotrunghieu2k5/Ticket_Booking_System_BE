spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/ticket_booking
    username: postgres
    password: dthxhieu

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  flyway:
    enabled: true

  jwt:
    secret: your-very-long-secret-key-at-least-256-bits
    access-token-expiration: 900000
    refresh-token-expiration: 604800000

  mail:
    host: smtp.gmail.com
    port: 587
    username: daotrunghieup@gmail.com
    password: dpsa uubx fjxj lfgd

    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

payos:
  client-id: ${PAYOS_CLIENT_ID:payos-client-id-placeholder}
  api-key: ${PAYOS_API_KEY:payos-api-key-placeholder}
  checksum-key: ${PAYOS_CHECKSUM_KEY:payos-checksum-key-placeholder}
  return-url: ${PAYOS_RETURN_URL:http://localhost:5173/payment/success}
  cancel-url: ${PAYOS_CANCEL_URL:http://localhost:5173/payment/cancel}