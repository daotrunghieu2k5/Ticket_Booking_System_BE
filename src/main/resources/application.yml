spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/ticket_booking
    username: postgres
    password: dthxhieu

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  flyway:
    enabled: true